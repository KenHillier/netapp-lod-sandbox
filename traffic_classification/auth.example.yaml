# Copy to auth.local.yaml (gitignored).
# host: grid ip or hostname
# username: grid admin username
# insecure: true for self-signed lab certs
# ca_bundle: optional path to a CA bundle

host: 192.168.0.80
username: root
insecure: true
# ca_bundle: /path/to/storagegrid-ca.pem
